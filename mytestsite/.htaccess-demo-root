# .htaccess file for /demo/ directory
# This redirects /demo/ to /demo/app/public/

<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /demo/

# Redirect /demo/ to /demo/app/public/
RewriteCond %{REQUEST_URI} !^/demo/app/public/
RewriteRule ^(.*)$ app/public/$1 [L]
</IfModule>
